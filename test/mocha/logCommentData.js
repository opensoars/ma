var assert = require('assert');

var base_dir = __dirname + '/../..',
    lib_dir = base_dir + '/lib/';


// inline em with a mocha test so we can check returns
process.log = function (){

};

// inline em with a mocha test so we can check returns
process.logErr = function (){

};
